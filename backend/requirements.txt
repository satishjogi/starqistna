# Star Qistna backend — minimal direct dependencies only.
# pip will resolve transitive deps automatically. To regenerate
# a frozen lockfile if ever needed: pip freeze > requirements.lock.txt

# Web framework + server
fastapi==0.110.1
uvicorn[standard]==0.25.0
gunicorn==26.0.0

# Database
motor==3.3.1
pymongo==4.5.0

# Auth & security
bcrypt==4.1.3
PyJWT==2.12.1
pyotp==2.9.0

# Validation
pydantic[email]==2.12.5

# Caching (in-process TTL)
cachetools==7.0.6

# HTTP client (used in Google OAuth session callback)
httpx==0.28.1

# Payments
stripe==15.0.1
emergentintegrations==0.1.0

# Email (Resend) + QR codes for tickets
resend==2.29.0
qrcode==8.2

# Config
python-dotenv==1.2.2
